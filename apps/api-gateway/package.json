{
  "name": "@vc-platform/api-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@fastify/cors": "latest",
    "@better-auth/api-key": "latest",
    "@vc-platform/database": "workspace:*",
    "@vc-platform/ledger-client": "workspace:*",
    "@vc-platform/types": "workspace:*",
    "better-auth": "latest",
    "dotenv": "latest",
    "drizzle-orm": "latest",
    "fastify": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
